# Metadiv Service Basic Structure

All service organize in service folder. Here is the example of `UserService`.

`UserService.go`

```go
package service

var UserService = new(userService)

type serviceService struct{}
```